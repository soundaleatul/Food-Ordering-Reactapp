{
  "name": "foodorderapp",
  "version": "1.0.0",
  "description": "Online Food Order Application",
  "scripts": {
    "start": "parcel index.html",
    "build": "parcel build index.html",
    "test": "jest"
  },
  "author": "Atul",
  "license": "ISC",
  "dependencies": {
    "@reduxjs/toolkit": "^2.3.0",
    "foodorderapp": "file:",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-redux": "^9.1.2",
    "react-router-dom": "^6.26.1"
  },
  "devDependencies": {
    "parcel": "^2.12.0",
    "postcss": "^8.4.49",
    "process": "^0.11.10",
    "tailwindcss": "^3.4.15"
  },
  "Browserslist": [
    "last 2 chrome versions",
    "last 2 firefox versions",
    "cover 100%, not chrome < 15, not and_chr < 15, not safari < 5, not ios_saf < 5, not firefox < 45, not and_ff < 45, not edge < 12, not ie < 9, not ie_mob < 9, not opera < 12.1, not op_mob < 12.1, not op_mini < 12.1, not android < 4.4"
  ]
}
